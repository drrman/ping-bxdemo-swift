import SwiftUI

struct DaVinciFormView: View {
    var body: some View {
        VStack {
            Text("DaVinci Form Placeholder")
                .font(.headline)
                .foregroundColor(.secondary)
        }
        .padding()
    }
}

#Preview {
    DaVinciFormView()
}
